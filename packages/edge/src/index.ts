/**
 * Everything you need to build an Edge-ready application.
 * @packageDocumentation
 * @module @mvdlei/edge
 * @preferred
 * @example
 * ```ts
 * import { Edge } from '@mvdlei/edge';
 * const edge = new Edge();
 *
 * export const { POST } = edge.api.handle((req, res) => {
 *    return res.json({ message: 'Hello, World!' });
 * })
 *
 * ```
 */
export class Edge {
  // ...
}
